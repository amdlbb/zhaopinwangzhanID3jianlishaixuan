spring:
  application:
    name: webservice
  profiles:
    active: dev
  cloud:
    nacos:
      server-addr: localhost:8848
      discovery:
        cluster-name: mycluster
#        ephemeral: false #设置非临时实例
      config:
        file-extension: yaml #配置文件后缀名