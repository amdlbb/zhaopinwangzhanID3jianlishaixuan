import {} from "@/api"
// state：仓库存储数据的地方
/*todo
*  ai客服的store完善
* */
const state = {
    ChatInfo: {}
}
// mutations：修改state的唯一手段，因此第一个参数必须是state，第二个参数为将要修改的数据
const mutations = {
}
// action：书写业务逻辑（method方法）
const actions = {
    // 获取用户信息

}
// getters：计算属性
const getters = {

}

export default {
    state,
    mutations,
    actions,
    getters
}